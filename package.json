{
  "name": "schema-form-studio",
  "version": "1.0.0",
  "private": true,
  "description": "JSON Schema visual form builder with bidirectional sync, live form preview and versioned document persistence",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "test": "vitest run",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "supertest": "^7.0.0",
    "vitest": "^2.1.9",
    "@types/supertest": "^6.0.2",
    "@types/node": "^20.17.0",
    "typescript": "^5.7.3"
  }
}
